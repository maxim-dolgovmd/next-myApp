'use client'

import React from "react"
import styled from "styled-components"

const ContainerFooter = styled.div`
    max-width: 1300px;
    width: 100%;
    padding: 0 60px;
    box-sizing: content-box;
    margin: 0 auto;
    flex: 0 0 auto;
`

export default React.memo(ContainerFooter)